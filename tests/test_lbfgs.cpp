#include <gtest/gtest.h>
#include <optlib/methods/lbfgs.hpp>

namespace optlib::test {

// TODO: add tests once lbfgs() is implemented
//
// TEST(LBFGS, Quadratic) {
//     // f(x) = x^T A x, minimum at 0
//     Matrix A = Matrix::Identity(10, 10);
//     auto f = [&](const Vector& x) { return x.dot(A * x); };
//     ...
// }

} // namespace optlib::test
